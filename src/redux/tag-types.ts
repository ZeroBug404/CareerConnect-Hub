export enum tagTypes {
  user = "user",
  jobs = "jobs",
  company = "company",
  project = "project",
}

export const tagTypesList = [tagTypes.user, tagTypes.jobs, tagTypes.company, tagTypes.project];
